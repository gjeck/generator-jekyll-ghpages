language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jekyll-ghpages' ]; then cd .. && eval "mv $currentfolder generator-jekyll-ghpages" && cd generator-jekyll-ghpages; fi
